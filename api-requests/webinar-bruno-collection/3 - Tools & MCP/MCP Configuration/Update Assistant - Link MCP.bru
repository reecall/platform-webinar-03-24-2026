meta {
  name: Update Assistant - Link MCP
  type: http
  seq: 1
}

patch {
  url: {{baseUrl}}/conversational/assistants/{{assistantId}}
  body: json
  auth: inherit
}

body:json {
  {
    "mcpIds": ["{{mcpId}}"]
  }
}
