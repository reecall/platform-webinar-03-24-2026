meta {
  name: Get Knowledge Base
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/ai/knowledgeBases/{{knowledgeBaseId}}
  body: json
  auth: inherit
}

body:json {
  {
    "name": "FAQ Cabinet Médical Saint-Martin",
    "description": "Questions fréquentes des patients : horaires, parking, accessibilité, motifs de consultation",
    "projectId": "{{projectId}}"
  }
}

script:post-response {
  bru.setVar("knowledgeBaseId", res.body.id);
}
